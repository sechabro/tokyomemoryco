---
import { Image } from 'astro:assets';
import sealLight from '../assets/tmc_round_alpha.png';
import sealDark from '../assets/tmc_round_alpha.png';

interface Props {
  class?: string;
}
const { class: className } = Astro.props;
const base = className ? `${className} ` : '';
---

{/* Both variants are rendered; CSS shows the one matching the active theme. */}
<Image
  class={`${base}brand__mark--light`}
  style="width: 180px; height: auto; max-width: none;"
  src={sealLight}
  alt=""
  widths={[200, 400, 600]}
  sizes="(max-width: 1023px) 118px, 158px"
  loading="eager"
  aria-hidden="true"
/>
<Image
  class={`${base}brand__mark--dark`}
  style="width: 180px; height: auto; max-width: none;"
  src={sealDark}
  alt=""
  widths={[200, 400, 600]}
  sizes="(max-width: 1023px) 118px, 158px"
  loading="eager"
  aria-hidden="true"
/>
